═══════════════════════════════════════════════════════════════════
                    🚀 COMMANDE FINALE VPS
═══════════════════════════════════════════════════════════════════

Ouvrez le terminal hPanel de votre VPS Hostinger et copiez-collez:

────────────────────────────────────────────────────────────────────

cd ~/apps/Metro && git stash push -m "Save-$(date +%Y%m%d-%H%M%S)" && rm -f fix-*.sh && git pull origin main && docker-compose build --no-cache backend && docker-compose up -d backend && sleep 15 && echo "✅ TERMINÉ!"

────────────────────────────────────────────────────────────────────

⏱️  Durée: 2-3 minutes

✅ Ensuite testez sur: https://beta-test-metro.mabstudio.fr
   → Instruments → Nouveau
   → Numéro série: TEST-OK
   → Nom: Test Final
   → Type et Site: VIDES
   → Créer ✅

═══════════════════════════════════════════════════════════════════

